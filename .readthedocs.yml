version: 2

build:
  image: latest

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
    - requirements: docs/rtd_requirements.txt

submodules:
  include: all

# Don't build any extra formats
formats: []
